[package]
name = "ship_gun"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# crates.io crates
nalgebra = "0.29.0"
parry2d = "0.7.0"
slab = { version = "0.4.3", features = ["serde"]}
tinyvec = { version = "1.2.0", features = ["serde"]}
# local crates
systems_core = { path = "../systems_core" }
ship_transform = { path = "../ship_transform" }
collision_check = { path = "../collision_check" }
square_map = { path = "../square_map" }
teams = { path = "../teams" }
hp_system = { path = "../hp_system" }
# external crates
# TODO collision should be part of this system
std_ext = { path = "../../../std_ext" }
sys_api = { path = "../../../sys_api" }
